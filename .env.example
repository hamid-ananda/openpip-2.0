DB_PASSWORD=openpip_dev
SECRET_KEY=django-insecure-dev-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:8080
# Leave EMAIL_HOST empty to print mail to the console instead of sending it.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@openpip.usask.ca
FRONTEND_URL=http://localhost:5173

# ORCID OAuth — register a public API client at https://orcid.org/developer-tools
# and add <site>/orcid/callback as a redirect URI. Leave blank to hide the button.
ORCID_CLIENT_ID=
ORCID_CLIENT_SECRET=
ORCID_BASE_URL=https://sandbox.orcid.org
